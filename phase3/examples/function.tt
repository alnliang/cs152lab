func add(int a, int b) {
    return a + b;
}

func mul(int func, int b) {
     return a * b;
}

func main() {
    int func;
    int b;
    a = add(10, 2);
    clog(a);
    b = mul(func, a + b);
    clog(b);
}
